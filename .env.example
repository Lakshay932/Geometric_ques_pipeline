# Copy to .env and fill in. All vars are prefixed VRE_ (see common/config.py).

# "stub" until a real backend is wired in (Phase 1)
VRE_VLM_PROVIDER=stub
VRE_LLM_PROVIDER=stub

# OpenRouter (https://openrouter.ai) — one key, many swappable models
VRE_OPENROUTER_API_KEY=
VRE_OPENROUTER_VLM_MODEL=
VRE_OPENROUTER_LLM_MODEL=
