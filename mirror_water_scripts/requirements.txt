# Geometry & transforms
numpy>=1.26,<2.0
shapely>=2.0,<3.0

# Rendering
pillow>=10.0,<11.0
svgwrite>=1.4,<2.0

# Schemas / data contracts
pydantic>=2.6,<3.0

# Serving layer
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# Generate -> verify -> repair control flow
langgraph>=0.0.40
langchain>=0.1.16

# Vector store (retrieval layer, Phase 4)
faiss-cpu>=1.8,<2.0

# Batch orchestration (Phase 3)
prefect>=2.16,<3.0

# Dataset storage
pyarrow>=17.0,<20.0

# Swappable VLM/LLM providers via OpenRouter (OpenAI-compatible API)
openai>=1.14,<2.0
httpx>=0.27,<1.0

# Config / env
python-dotenv>=1.0,<2.0
pydantic-settings>=2.2,<3.0

# Structured logging
structlog>=24.1,<25.0

# Testing
pytest>=8.0,<9.0
